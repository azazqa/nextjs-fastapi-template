# Project root — used by docker compose variable substitution (${VAR}).
# Copy via `make init` (do not commit .env).

COMPOSE_PROJECT_NAME=nextjs-fastapi-template
TZ=Asia/Seoul

POSTGRES_USER=postgres
POSTGRES_PASSWORD=__POSTGRES_PASSWORD__
POSTGRES_DB=app
POSTGRES_TEST_PASSWORD=test
